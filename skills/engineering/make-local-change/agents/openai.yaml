interface:
  display_name: "Make Local Change"
  short_description: "Keep one feature local until sharing is approved"
  default_prompt: "Use $make-local-change to keep this feature local and ask before changing shared code."

policy:
  allow_implicit_invocation: false
