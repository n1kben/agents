interface:
  display_name: "Local Change"
  short_description: "Implement one feature without premature sharing"
  default_prompt: "Use $local-change to implement this feature without creating shared code."

policy:
  allow_implicit_invocation: false
